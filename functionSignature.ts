//  FUNCTION SIGNATURE // 

// let greet: Function 

// greet = () => console.log('Hello world')

// greet()

// let greet: (a: string, b: string) => void 
// greet = (name: string, greeting: string) => {
//     console.log(`${name} says ${greeting}`)
// }
// greet()

// let calculate: (a: number, b: number, c: string) => number
// calculate = (so1: number, so2: number, action: string) =>
//  action === 'add' ? so1 + so2 : so1 - so2

// // console.log(calculate(4,2,'add'))

// type  Student = {name: string; age: number}

// let printStudent: (student: Student) => void 
// printStudent = ({name,age}: Student) => 
//     console.log(`${name} is ${age} years old`)

// printStudent({name: 'Hoang', age: 24})