// // type alias

// type StringOrNumber = string | number;
// type Student = {name: string;
//  id: string | number;
//   age: number};

// const studentDetails = (id: StringOrNumber, studentName: string): void => {
//     console.log(`Student ${id} has name is ${studentName}`)
// }

// // studentDetails(123,'Henry')
// // studentDetails('123','Henry')

// const greet = (user: Student) => {
//     console.log(`${user.name} with id ${user.id} and ${user.age} tuoi says hello`)
// }

// greet({name: 'trung', id: 123, age: 23})